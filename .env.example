# Obligatoire en production
OPENAI_API_KEY=sk-...
JWT_SECRET=changez-ce-secret-en-production-32-chars-min

# Optionnel
PORT=3000
NODE_ENV=development

# Pour Railway/Render (PostgreSQL) — laissez vide pour SQLite local
# DATABASE_URL=postgresql://user:password@host:5432/medivoix
